package com.example.walkingpark.data.model.mapper

class AirMapper {

/*    fun transform(response: AirResponse.Response.Body) : Weathers {
        return with(response) {
            Weathers(
                total = totalCount,
                page = pageNo,
                weathers = response.items.map {
                    Weathers.Weather(
                        id = 0,
                        baseDate = it.baseDate,
                        baseTime = it.baseTime,
                        category = it.category,
                        showDate = it.fcstDate,
                        showTime = it.fcstTime,
                        nx = it.nx,
                        ny = it.ny
                    )
                }
            )
        }
    }*/
}